// TODO(auth-onboarding): full implementation — registration form (e-post, passord) → onboarding flow.

export default function RegistrerPage() {
  return (
    <main className="mx-auto max-w-content px-6 py-16">
      <h1 className="text-3xl font-semibold">Registrer deg</h1>
      <p className="mt-4 text-fg-muted">
        Skjema for ny bruker kommer her.
      </p>
    </main>
  );
}
