// TODO(auth-onboarding): full implementation — landing page (hero, CTA, etc.).
// This stub exists so the navigation-shell capability has somewhere to route to.

export default function LandingPage() {
  return (
    <main className="mx-auto max-w-content px-6 py-16">
      <h1 className="text-3xl font-semibold">Boligscore</h1>
      <p className="mt-4 text-fg-muted">
        Score boliger sammen med husstanden din.
      </p>
    </main>
  );
}
