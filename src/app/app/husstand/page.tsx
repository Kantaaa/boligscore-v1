// TODO(households): full implementation — household members, invites, roles.

export default function HusstandPage() {
  return (
    <section aria-labelledby="husstand-heading" className="space-y-4">
      <h1 id="husstand-heading" className="text-2xl font-semibold">
        Husstand
      </h1>
      <p className="text-fg-muted">
        Her vil medlemmer og invitasjoner vises.
      </p>
    </section>
  );
}
